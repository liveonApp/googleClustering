import 'package:flutter/material.dart';

class CalculateHealthCheckScreen extends StatelessWidget {
  const CalculateHealthCheckScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(),
    );
  }
}
